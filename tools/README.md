# tools/

Допоміжні скрипти. У грі не використовуються — лише в редакторі й у терміналі.

| Файл | Що робить |
|---|---|
| `voxelize.py` | Meshy `.glb`/`.obj` → воксельні частини героя `data/voxels/*_ai.json` + `.vox` |
| `vox2json.py` | Зворотно: MagicaVoxel `.vox` → `data/voxels/<name>.json` |
| `test_voxelize.py` | Тести конвертера на синтетичному звірятку (`python3 tools/test_voxelize.py`) |
| `rig_fix.py` | Blender headless: прибрати зайві кістки з ригнутого `.glb` і віддати їхні ваги сусідній кістці (`--list` — тільки дамп дерева з вершинами) |
| `prop_prepare.py` | Blender headless: привести згенеровану модель до вимог гри — опустити початок координат у низ, змасштабувати до потрібної висоти. Правки йдуть у ВЕРШИНИ, не в батьківський вузол (PropLibrary бере меш і трансформ батька ігнорує).<br>**Повний прохід для моделі з генератора:** `--box ШxВxГ --origin bottom --no-rig --weld --tris 3000 --matte --tex-size 1024`. `--weld` обов'язковий перед `--tris`: glTF розщеплює вершину на кожному шві UV, і без зварювання спрощення рве дах на клапті замість того, щоб його спрощувати. Стелю перевіряє `tests/test_prop_budget.gd`.<br>Карта нормалей типово ЛИШАЄТЬСЯ (`--no-normal` її прибирає): дрібний рельєф переживає спрощення сітки лише в ній |
| `project_texture.py` | Blender headless: вдягнути модель у КОНЦЕПТ-КАРТИНКУ, з якої її згенерували — планарна проєкція. Потрібен вигляд СПЕРЕДУ або ЗБОКУ на рівному фоні: з ракурсу 3/4 фон тече на модель. Уміє і вершинні кольори замість текстури |
| `mesh_smooth_uv.py` | Blender headless: прибрати з моделі деталь, задану рамкою в ТЕКСТУРІ (Meshy іноді ліпить очі просто в геометрію). Уміє підмінити базову текстуру / normal / roughness, а за потреби й розгладити меш. **Типово бери `--no-geometry`**: «опуклість» деталі зазвичай малює normal map, а не геометрія, і різати меш із багатьох окремих панелей означає рвати шви |
| `eye_similarity.py` | Порівняти рендер ока з референсною текстурою числами (`python3 tools/eye_similarity.py наше.png референс.png`) — див. skill `visual-check` |
| `prop_similarity.py` | Наскільки оброблена модель схожа на вихідник, у відсотках: `python3 tools/prop_similarity.py вихідник.glb у_грі.glb`. Рахує СИЛУЕТ (перетин обрисів) і КОЛІР на розмірі, з якого річ видно в грі, і бере гірше. **Це сито, а не вирок:** запікання зсуває дрібний візерунок, і листяні форми воно занижує — остаточно вирішує око по аркушу пар |
| `prop_sheet.py` | **Один аркуш з усіма моделями гри**, з однакового ракурсу й підписані вагою: `python3 tools/prop_sheet.py --out docs/refs/prop_sheet.png --cols 7 --cell 230`. Модель понад стелю (4000 гр. / 2,5 МБ) обводиться червоним. Так одразу видно, що якась модель приїхала важкою, кривою або в чужому стилі |
| `prop_budget_search.py` | Знайти НАЙМЕНШУ кількість граней, за якої модель ще не псується: `python3 tools/prop_budget_search.py assets/props/barrel_*.glb`. Знімає модель на кількох бюджетах і бере перший, де змінилось менше за `--max-diff` (типово 3%) площі МОДЕЛІ. Обирати цифру оком не можна — так у гру поїхав зіпсований місток |
| `prop_render.py` | Знімок моделі з фіксованого ракурсу (камера, світло, фон завжди однакові), щоб порівнювати моделі числом |
| `prop_bake.py` | **Ретопологія із запіканням** (Blender headless). Робить із важкої моделі грубу ціль, розгортає її наново й запікає на неї поверхню оригіналу: колір → базова текстура, нахил → карта нормалей. Саме так дрібний рельєф (черепиця, дошки, цегла) переїздить із геометрії в текстуру. `--in --out --tris 2000 --size 1024 --box ШxВxГ --cage 0.03`.<br>`--shape sphere` бере за ціль просту кулю замість спрощеної сітки — для форм із багатьох окремих оболонок (кущ, крона), де схлопування впирається в дно: кущ 104 299 гр. → **200 гр., 0,12 МБ**. Перевірено на house_terra: 30 918 гр. / 34,4 МБ → 1 998 гр. / 0,45 МБ, вигляд збережено |
| `mesh_lod.gd` | Спрощення сітки через meshoptimizer, вбудований у Godot (`ImporterMesh.generate_lods`): `SRC= OUT= TRIS= godot --headless --path . -s tools/mesh_lod.gd`. Береже шви краще за Blender, але й спиняється раніше — на моделі з окремих оболонок нижче половини не йде (див. `docs/tasks/house-retopo.md`) |
| `localize_chunk_z.py` | Одноразова міграція: маркери чанків з абсолютних метрів рівня в локальні, зсув переїжджає в `LevelLayout.z_offset_m`. Зі вбудованою звіркою |
| `../src/debug/chunk_shot.tscn` | Знімок чанка без редактора: `OUT=/tmp/c.png LEVEL=1 CHUNK=1 VIEW=top FROM=0 SPAN=50 godot --path . res://src/debug/chunk_shot.tscn`. `VIEW=top` — карта згори (дорога впоперек кадру), `VIEW=run` — з-за спини героя |
| `palette_hero.json` | Палітра героя `{символ: #hex}` для `vox2json.py --palette` |
| `perf/` | Сцена заміру FPS (Godot) |
| `../src/debug/face_shot.tscn` | Знімок морди героя анфас + тісний кроп ока: `OUT=/tmp/face.png HERO=lys godot res://src/debug/face_shot.tscn` |
| `../src/debug/zoo.tscn` | Усі звірята в ряд, живі, спільні клавіші: `godot res://src/debug/zoo.tscn` (`ZOO=all` — ще й воксельні, `ZOO=lys,olen` — вибірка, `OUT=/tmp/zoo.png` — знімок і вихід) |
| `../src/debug/shatter_shot.tscn` | Знімок РОЗБИТТЯ предмета: `OUT=/tmp/sh KIND=stump WORLD=meadow FRAMES=40 godot --path . --fixed-fps 60 res://src/debug/shatter_shot.tscn`. `--fixed-fps` обов'язковий: кожен кадр пишеться в PNG, і без фіксованої дельти ефект старіє на секунду за вісім кадрів |
| `../src/debug/model_preview.tscn` | Показати героя зі скелетним ригом (`RIG=<ім'я>`) або, як запасний варіант, один воксель у порожній сцені: `VOXEL=<ім'я> godot res://src/debug/model_preview.tscn` |
| `shots/` | Сцена автознімків (Godot) |
| `probe/` | Запитати живу гру числами: геометрія всіх Control'ів + кадр З ІНТЕРФЕЙСОМ — див. розділ нижче |
| `bridge-run.sh` | Те саме, але інтерактивно через MCP-міст. Потрібен рідко — див. розділ нижче |

## Запитати живу гру (`probe/`) — типовий спосіб

```bash
OUT=/tmp/probe STAGE=level LEVEL=1 FRAMES=300 \
  /Applications/Godot.app/Contents/MacOS/Godot --path . --fixed-fps 60 res://tools/probe/probe.tscn
```

Пише `OUT/probe.json` — **кожен Control сцени з глобальним прямокутником, текстом і
видимістю**, плюс fps, кадр у мс і draw calls — і `OUT/frame.png`, **кадр разом з
інтерфейсом**. Далі це читається скриптом і порівнюється числами, як велить skill
`visual-check`.

Змінні: `STAGE` (`menu` типово / `level`), `LEVEL`, `FRAMES`, `OUT`, `RESET=1` (знести
`user://save.json` перед заміром), `RESIZE=1600x900` (змінити розмір вікна й переміряти —
у `probe.json` з'явиться `hud_after_resize`).

**Запускати БЕЗ `--headless`.** У headless Godot не малює нічого, тож UI на знімку не буде —
саме через це довго здавалося, ніби HUD можна дістати лише мостом MCP. Насправді достатньо
звичайного вікна й `get_viewport().get_texture()`, як у `shots/`. Міст для цього не потрібен.

**Детермінізм — як у `shots/`:** те саме зерно, `--fixed-fps 60`, чекати кадрами, а не
таймерами.

**Проба бачить ВАШ збережений прогрес** (`user://`), тому без `RESET=1` стартовий екран
буває різним: із чистим записом це заставка, з пройденими рівнями — одразу мапа. Для
порівнянь «до/після» завжди беріть `RESET=1`.

**Чим це вже пригодилось.** `RESIZE` викрив ваду в `_apply_safe_area`: відступ під виріз
камери падав 27.84 → 26.76 після ОДНІЄЇ зміни розміру вікна, бо в розрахунок ішов уже
підтиснутий `_root.size`. Тобто HUD підповзав під «чубчик» на кожному повороті екрана —
на настільному Godot цього не побачити взагалі. Стереже тепер
`tests/test_hud_layout.gd::test_safe_area_padding_does_not_creep_when_reapplied`.

## Міст MCP до живої гри (`bridge-run.sh`) — лише для інтерактиву

Потрібен **лише тоді, коли треба длубатися в грі на ходу**: поставити питання, побачити
відповідь, поставити наступне. Для вимірів і знімків беріть `probe/` вище — він автоматичний,
детермінований, нічого не лишає в проєкті й не потребує ні дозволів, ні участі людини.



```bash
tools/bridge-run.sh          # закрили вікно гри — міст зник сам
```

Кладе `mcp_bridge.gd` у корінь, реєструє автозавантаження `MCPBridge`, запускає гру — і на
виході прибирає обидва. Прибирання висить на `trap`, тож стається й після Ctrl-C, і після
падіння гри; якщо прибрати не вдалось, скрипт кричить і повертає ненульовий код.

**Навіщо.** Поки гра працює з мостом, агент читає її на ходу: дерево сцени, властивості
вузлів, **точну геометрію кожного елемента HUD із текстом** і справжні знімки кадру
**разом з інтерфейсом**. Останнє інакше недосяжне: у headless Godot 2D не малює взагалі,
тож HUD доти не було чим міряти, окрім ока. Див. skill `visual-check`.

**Чому міст не живе в репозиторії.** Захисту від релізної збірки в ньому немає — забутий
міст поїде в магазин із відкритим портом 9081. Тому він ставиться на один сеанс і зникає.
Стереже `tests/test_no_dev_bridge_shipped.gd`: поки міст стоїть, цей тест **червоний** —
це сигналізація, а не поломка.

**Порт прибитий до 9081** (`GODOT_MCP_BRIDGE_PORT`). Без цього гра обирає стартовий порт
випадково в 9081–9090, а MCP-клієнт стукає в 9081 — і вони не зустрічаються
(«Bridge secret not found»). Реєстр інстансів, який мав би їх познайомити, заповнюється лише
тоді, коли гру запускає сам MCP-сервер.

**Запускає людина, не агент.** Дозвільний шар Claude Code навмисно не дає агентові ні
ставити міст, ні запускати гру, коли міст уже стоїть: відкритий командний сокет — це канал
керування повз цей самий шар. Знімати міст агентові дозволено (`game_bridge_uninstall`), бо
це зменшує поверхню, а не створює її. Обходити ці блоки — навіть цим скриптом — не можна:
він для того, щоб людині було зручно, а не щоб сховати заборонену дію під іншим ім'ям.

Змінні: `GODOT` — шлях до бінарника (типово `/Applications/Godot.app/Contents/MacOS/Godot`,
у PATH його немає), `GODOT_MCP_BRIDGE_PORT` — інший порт, якщо 9081 зайнятий.

## Воксельний пайплайн

```bash
pip3 install trimesh numpy
python3 tools/voxelize.py docs/refs/models/fox.glb --hero lys --height 0.98 --pitch 0.04 --dry-run
python3 tools/voxelize.py docs/refs/models/fox.glb --hero lys --height 0.98 --pitch 0.04 \
    --out data/voxels --vox docs/refs/models/out/
```

Покрокова інструкція (і таблиця «що робити, коли негарно») — `docs/tasks/voxelize.md`.

## Скелетна модель замість вокселів (без інструментів)

`.glb` зі **скелетом** воксeлізувати не треба взагалі: скопіюй його в `assets/models/`,
допиши герою `"rig": "<ім'я файлу без .glb>"` у `data/heroes.json` — і `Hero3D` малює
цю модель, анімуючи кістки процедурно й фарбуючи вершини за зонами
(`src/run3d/hero_rig.gd`). Покрокова інструкція — `docs/tasks/rig.md`.

Єдиний інструмент, який тут буває потрібен, — `rig_fix.py`, коли Meshy повісив шкіру
лапки на чуже пасмо чи аксесуар:

```bash
alias blender=/Applications/Blender.app/Contents/MacOS/Blender

# 1. подивитись дерево кісток і вершини на кожній (нічого не змінює)
blender --background --python tools/rig_fix.py -- --in assets/models/unicorn.glb --list

# 2. прибрати зайві кістки, ваги віддати найближчій задній лапці (пише .glb.bak)
blender --background --python tools/rig_fix.py -- \
    --in assets/models/unicorn.glb --out assets/models/unicorn.glb \
    --remove Bone_025 Bone_024 Bone_023 Bone_028 Bone_027 Bone_026 \
    --merge-into nearest-leg --report

# 3. переімпортувати й глянути
godot --headless --import
RIG=unicorn godot res://src/debug/model_preview.tscn
```

`--merge-into parent` — віддати ваги батькові видаленої кістки; `--legs` — свій список
ланцюжків для `nearest-leg`; `--dry-run` — не писати файл. `heroes.json` скрипт не чіпає:
що саме прибрати з `rig_bones` (`static`, зайві імена в `tail`), написано в
`docs/tasks/rig.md`, розділ «Чистка рига в Blender».

## Готовий воксельний GLB (`--exact`)

Коли `.glb` уже **воксельний** (кубики на рівній сітці — вихід MagicaVoxel, Blender-remesh
чи онлайн-воксeлізатора), проганяти його через `trimesh.voxelize` шкідливо: сітка «попливе»,
а кольори загубляться. Режим `--exact` сітку **знаходить**, а не будує.

```bash
# 1. подивитись (нічого не пишеться)
python3 tools/voxelize.py docs/refs/models/fox_voxel.glb --exact --name fox_voxel \
    --height 0.98 --out data/voxels --vox docs/refs/models/out/ --dry-run

# 2. те саме без --dry-run — пише data/voxels/fox_voxel.json і docs/refs/models/out/fox_voxel.vox
python3 tools/voxelize.py docs/refs/models/fox_voxel.glb --exact --name fox_voxel \
    --height 0.98 --out data/voxels --vox docs/refs/models/out/

# 3. глянути в грі, не чіпаючи heroes.json
VOXEL=fox_voxel godot res://src/debug/model_preview.tscn
```

Що робить `--exact`:

- **розмір кубика** = мода довжин ребер трикутників (запасний план — найменший крок габариту);
  друкується в консоль, задається вручну через `--cube-size`;
- **зайнятість** — центр кубика для кожної грані: `центроїд − нормаль × size/2` (бік усередину),
  індекси = `round((центр − bbox_min − size/2) / size)`. Паралельно рахується
  `trimesh.voxelize(pitch=size)` з тим самим початком; обидві кількості друкуються, береться
  снап граней (він точніший), а `trimesh` — лише якщо снап розсипався. Керується `--grid`;
- **кольори** — справжні: `vertex_colors` → мода кольорів вершин грані; текстура → `uv_to_color`
  в UV-центроїді грані; лише матеріал → його базовий колір; нічого — сірий `#B0B0B0`.
  Далі квантування (32 рівні на канал, топ-`--max-colors` = 16, решта до найближчого)
  й символи `a, b, e, f…` (`o d c k i t m` пропущені: їх Hero3D підміняє кольорами героя);
- `size` у JSON = знайдений розмір кубика після масштабування моделі до `--height` (типово 0.98);
- `.vox` пишеться **завжди зі справжніми кольорами**.

`--exact` типово працює з `--parts none` — один файл `<name>.json`, який показується як
`VoxelBuilder.instance("<name>")` (декор, звірятко, прев'ю). З `--parts hero` вмикається та сама
сегментація на знайденій сітці, але **без `--fit`**: частини лишаються зі справжньою кількістю
вокселів, а інструмент лише **звітує**, чи збігаються висоти з константами Hero3D. Кольори
лишаються справжніми; символи-зони `o/d/c/k/i/t` вмикає окремий прапорець `--zones`.

Ще прапорці: `--up y|z` (вимкнути автовизначення Z-up — у довгого звірятка глибина буває
більша за зріст), `--front`, `--name`.

**Формат виходу** збігається з `addons/mgp_core/voxel/voxel_builder.gd`:
`layers[y][z][x]` — шар це y (знизу вгору), рядок це z (перший рядок — **перед, -Z**),
символ у рядку це x. Герой дивиться в -Z (`Hero3D.FACE_Z = -0.425`).
Символи палітри Hero3D підміняє кольорами героя: `o` основний, `d` темніший,
`c` крем, `k` темне, `i` рожеве, `t` акцент, `m` плямки.

**Контракт габаритів — у метрах, а не в кількості вокселів (v2).** `--fit` (типово увімкнено)
приводить кожну частину до констант Hero3D із допуском в один воксель: голова 0,45 м заввишки
і 0,45 м завглибшки (2 × `HEAD_HALF_D` — інакше очі Hero3D потонуть у голові), тулуб 0,375 м,
лапка 0,225 м, вухо ≤ 0,22 м, хвіст рівно `TAIL_LEN` 0,35 м (коротший добивається порожніми
рядами ззаду, щоб Hero3D ставив шарнір за константою). Ширина всіх частин < 0,58 м.
Скільки в цих метрах вокселів — вирішує `--pitch`: **типово 0,04 м** (голова ≈ 11 шарів),
було 0,075 м (6 шарів) — саме через це деталі AI-моделі губились. Пропорції x/z тягнуться
за точною віссю, тож частина не плющиться. `tests/test_heroes_v15.gd` звіряє те саме в метрах.

`heroes.json` скрипти не редагують — блок `parts` друкується в консоль.
