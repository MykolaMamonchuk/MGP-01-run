# Біжи-біжи — для агентів

Гра для малят на Godot 4.7, мобільна, українською. Код, коментарі й повідомлення комітів —
українською.

## Спершу прочитай

**[docs/MEMORY.md](docs/MEMORY.md) — memory bank проєкту.** Уроки, які ми вже оплатили:
що сталось → правило → як перевірити. Читати **перед** задачею, а новий урок дописувати
того самого дня, коли він стався.

**[docs/tools-for-agents.md](docs/tools-for-agents.md) — шпаргалка з інструментів і порядок
дій, коли «щось не так із виглядом»: спершу порахуй геометрію, потім подивись на пікселі, і
лише потім міряй.**

**[docs/chunks.md](docs/chunks.md) — як улаштовані рівні.** Рівень — це список цеглинок із
`levels/chunks/`, а не тека з файлами. Там же числа, без яких не поставити маркер: де край
дороги на 3/5/7 доріжок і де починається канал.

Далі за потреби: [docs/README.md](docs/README.md) — що де лежить;
[tools/README.md](tools/README.md) — інструменти; [docs/MCP.md](docs/MCP.md) — MCP для Godot.

## Тести

`godot` **не в PATH**. Повний набір — 855 тестів, ~23 с:

```bash
G=/Applications/Godot.app/Contents/MacOS/Godot
$G --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/ -gexit     # усе
$G --headless -s addons/gut/gut_cmdln.gd -gtest=res://tests/test_safe_area.gd -gexit   # один файл, 2 с
```

Правка, що чіпає вигляд або розстановку, без тесту не вважається зробленою.

## Вигляд перевіряємо числами, не оком

Скіл `visual-check` — метод. Інструмент — [`tools/probe/`](tools/README.md): запускає гру
у справжньому вікні (**без** `--headless`, інакше 2D не малюється) і дає JSON з геометрією
всіх Control'ів плюс кадр з інтерфейсом.

Заміри «до/після» — лише з контрольним прогоном і по два рази на варіант; фіксуй `RESET=1`
і `PROFILE=`, інакше різні прогони йдуть із різною швидкістю і число бреше. Подробиці —
перший запис у memory bank.

## Межі

Установку мосту MCP, запуск гри з ним і правку власних дозволів дозвільний шар Claude Code
блокує навмисно. Це робить людина; обхід іншим інструментом — теж обхід. Деталі в
[docs/MCP.md](docs/MCP.md).
