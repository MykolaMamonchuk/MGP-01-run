# Код-рев'ю MGP гра 01 «Біжи-біжи» — 2026-09-06

Статус кожної знахідки після виправлень цього коміту.

| # | Знахідка | Статус |
|---|----------|--------|
| 1 | `Ground` (StaticBody2D) без `collision_layer` — герой (mask = 2) провалювався крізь землю | виправлено — `collision_layer = 2` у `src/run/run.tscn` |
| 2 | «Пауза» на станції та вві сні була фейковою: фізика, спавнер і таймер сесії продовжували працювати | виправлено — справжня `get_tree().paused`, `PROCESS_MODE_ALWAYS` для Run/HUD/ParentGate, пауза також на екрані батьків |
| 3 | `Events.obstacle_passed` не емітився після зіткнення — статистика AgeAdapt зміщувалась | виправлено — сигнал емітується завжди при проходженні (`src/run/obstacle.gd`) |
| 4 | Авто-допомога «присід» під гілку ніколи не відпускалась (герой лишався пригнутим) | виправлено — `_release_assist_duck()` через таймер 0.7 с |
| 5 | Модель вводу: натиск одразу стрибав, тож присід був недосяжний | виправлено — стрибок на відпусканні при утриманні ≤ 0.25 с, довше — присід |
| 6 | HUD-лічильник зірочок оновлювався до запису в збереження (показував старе число) | виправлено — `_on_star_collected()` → `call_deferred("_refresh")` |
| 7 | Тест таймера сесії проходив «випадково»: перевірки без індексів емісії; для 3-секундної сесії 120-попередження безглузде | виправлено — попередження «за 2 хв» лише коли `left > 60.0`; тести з індексами емісії + окремий тест повної сесії |
| 8 | CI: `-gconfig=tests/.gutconfig.json` — GUT не знаходив конфіг | виправлено — `-gconfig=res://tests/.gutconfig.json` (коментар про вендоринг GUT збережено) |
| 9 | Емодзі як єдиний носій смислу в HUD (не рендеряться на частині пристроїв, недоступні дітям) | виправлено — намальовані іконки `src/ui/icons.gd` (Star/Parent/Hand/Moon/Station), текст лише другорядний |
| 10 | AgeAdapt міряв реакцію від моменту спавну — перешкода ще за екраном, час завищений | виправлено — таймер `travel - 1 c` до «активації» загрози (`_arm_threat`) |
| 11 | AgeAdapt рахував будь-який `player_input` (зокрема тапи на станції/в меню) як реакцію | виправлено — новий сигнал `Events.gameplay_input`; невідповідь на загрозу пишеться як 2000 мс |
| 12 | Лямбда з `if` в одному рядку в `show_yawn()` — крихкий і невалідний стиль | виправлено — іменований `_end_yawn()` з `is_instance_valid` |
| 13 | Позіхання перезаписувало текст панелі станції | виправлено — окрема `yawn_panel` |
| 14 | `RectangleShape2D` героя спільний між інстансами + запис у форму щокадру | виправлено — `resource_local_to_scene = true`, запис лише при зміні висоти |
| 15 | Грошовий бар'єр батьків можна було «перебрати» перебором варіантів | виправлено — `_fails`, блокування на 30 с після 2 помилок, панель «Спробуйте пізніше» |
| 16 | `Ads.show_rewarded()` покладався на зовнішній виклик ParentGate і не рахував покази | виправлено — вбудований `ParentGate.request("settings", _do_show)` + `_record_view()` |
| 17 | Дизайн-рішення: `rewarded_or_stars` (нагорода за рекламу vs за зірочки) не визначене | відкладено → Jira |
| 18 | `SaveService._migrate()` не перевіряв форму даних — пошкоджений save валив гру | виправлено — валідація children/active_child/settings/full_game з відкатом до `default_data()` |
| 19 | Дублювання «10 хвилин» і нечіткий шлях налаштувань сесії | виправлено — єдине джерело: `SaveService.setting("session_minutes", 10)` |
| 20 | `session_minutes` бралися то з профілю віку, то з налаштувань батьків | виправлено — завжди з налаштувань батьків; ключ у `profiles.json` лишено з поміткою «на майбутнє» |
| 21 | Змішування `position` і `global_position` (зірочки, авто-допомога, перевірка «пройдено») | виправлено — усюди `global_position`; `hero_x` передається спавнером у `Obstacle.setup()` |
| 22 | `queue_redraw()` щокадру для зірочок і героя — зайве навантаження на слабких пристроях | відкладено → Jira |
| 23 | `checkpoint_seconds` читався з `profiles` щокадру (і з неправильним типом) | виправлено — читається один раз у `_ready()` |
| 24 | Вигадані `uid="uid://..."` у `.tscn` — конфлікт із UID, що згенерує редактор | виправлено — атрибути прибрано, `load_steps` перераховано |
| 25 | Параметри з іменем `name` перекривали `Node.name` | виправлено — `profile_name` / `sfx_name` / `music_name` / `child_name` |

## Відкладено (Jira)

- **#17** — потрібне продуктове рішення: чи давати нагороду за rewarded-рекламу паралельно зі шляхом «зібрати зірочки», і як це не перетворюється на тиск на дитину. До рішення `Ads.is_available()` лишається `false`.
- **#22** — оптимізація перемальовування (кешування у `MultiMesh`/`Texture2D` замість `_draw()` щокадру). Робити разом із заміною заглушок на арт із арт-конвеєра.

## Друга перевірка (Godot 4.7 headless)

Прогін на реальному рушії `Godot v4.7.stable` у headless-режимі: імпорт проєкту, запуск сцени бігу, повний набір тестів GUT.

| # | Знахідка | Статус |
|---|----------|--------|
| 1 | `AgeAdapt.decide()`: `float(collisions) / max(1, obstacles)` — правий операнд `int`, ділення цілочисельне, `rate` завжди 0 або 1 | виправлено — `float(collisions) / float(max(1, obstacles))` |
| 2 | `run.gd::_on_release()`: короткий тап після присіду не знімав `set_duck(false)` — герой лишався пригнутим | виправлено — `hero.set_duck(false)` викликається безумовно **до** гілки тап/утримання; короткий тап далі дає стрибок + sfx |
| 3 | `AgeAdapt._arm_threat()`: таймер тримав сильне посилання на перешкоду і спрацьовував уже після `_reset_window()`, псуючи статистику наступного вікна | виправлено — біндиться `weakref(node)` і `_window_id`; `_reset_window()` інкрементує `_window_id`, застарілий таймер виходить одразу |
| 4 | `ParentGate.make_question()`: `options.shuffle()` бере глобальний RNG, тож `rng_seed` не давав відтворюваного питання | виправлено — сіяне тасування Фішера–Йейтса на `rng.randi_range(0, i)` |
| 5 | `tests/test_session_timer.gd`: доданий у дерево таймер тікав власним `_process` паралельно з ручними `tick()` — час у тесті йшов удвічі | виправлено — `_timer.set_process(false)` одразу після `add_child_autofree()` |
| 6 | `tests/test_heroes_data.gd`: після `assert_not_null(f, ...)` виконання йшло далі й падало на `f.get_as_text()`, якщо файлу немає | виправлено — `if f == null: return` після асерту |
| 7 | GUT не був завендорений — тести неможливо було запустити ні локально, ні в CI | виправлено — `addons/gut` з `github.com/bitwes/Gut`, разом із `LICENSE.md` |
| 8 | GUT 9.4.0 несумісний із Godot 4.7: `gut_loader.gd::_static_init()` присвоює `Nil` у `bool` (`ProjectSettings.get("debug/gdscript/warnings/exclude_addons")` не зареєстровано поза редактором) → `SCRIPT ERROR` на кожному прогоні | виправлено — завендорено GUT **9.7.1**, де цей код прибрано; вендор лишається немодифікованим |
| 9 | `CREDITS.md` не містив рядка про сторонню залежність GUT | виправлено — додано рядок GUT 9.7.1 / bitwes / MIT / «unit tests (dev only)» |
| 10 | Імпорт проєкту на 4.7 не перевірявся | перевірено — `--headless --import .` завершується кодом 0, без `SCRIPT ERROR` / `Parse Error` |
| 11 | Запуск сцени бігу на 4.7 не перевірявся | перевірено — `--headless --path . --quit-after 120` завершується кодом 0, у виводі жодного рядка `error` / `warning` / `script` |

### Команди й результат

```
$GODOT --headless --import .                      → exit 0, 0 помилок
$GODOT --headless --path . --quit-after 120       → exit 0, 0 помилок/попереджень
$GODOT --headless --path . -s addons/gut/gut_cmdln.gd \
       -gconfig=res://tests/.gutconfig.json -gexit → exit 0
```

GUT: **Scripts 5 · Tests 17 · Passing 17 · Asserts 118 · All tests passed.**
