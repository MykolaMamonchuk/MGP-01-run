# Задачі — Спринт 1 (дзеркало Jira MGPP1)

| Key | Задача | Статус | Готово, коли |
|---|---|---|---|
| MGPP1-13 | Назва бренду/героя | To Do | Назву і головного героя затверджено |
| MGPP1-14 | Концепт героя | To Do | Є візуальний концепт героя (скетч/референс) |
| MGPP1-15 | Палітра і шрифт | **Done** | Палітра — `src/ui/theme/palette.gd`; шрифт — Balsamiq Sans (OFL), поставлено 17.09.2026, стереже `tests/test_font.gd` |
| MGPP1-16 | GDD | Review | GDD у Confluence узгоджено з командою |
| MGPP1-17 | Репозиторії/скелет | In Progress | Репозиторій і базова структура проєкту готові |
| MGPP1-18 | Прототип біг/стрибок/перешкоди | In Progress | Герой біжить, стрибає, є перешкоди й зіткнення |
| MGPP1-19 | Авто-вік з даних | In Progress | AgeAdapt читає профілі з data/ і перемикає складність |
| MGPP1-20 | Плейтест з доньками | To Do | Проведено плейтест, зібрано зворотний зв'язок |

## Борг збірки v0.5.0

- Аудіо — досі нема файлів (усі ~40 назв sfx/voice уже викликаються з коду; список: `grep -o 'sfx("[a-z_]*")' -r src addons | sort -u`).
- Локалізація EN: усі рядки українською в коді — винести в `data/i18n/*.csv`.
- Щоденні завдання (3 на день) і альбом стікерів — не реалізовано (є завдання на рівень).
- Подія «міст падає» (hop) — не реалізовано.
- Ріст героя (стадії 2–3).
- Механіка Самокат: рейка поки бонус, а не «ковзання по рейці»; Невагомість: хмаринки-платформи поки як перешкоди «убік».
- Фінальні титри після 17-го рівня.
- Draw calls: злити ряди дороги в MultiMesh → [OPT-01](optimisation/2026-09-07-render-budget.md#opt-01).

- Аудіо: файлів немає (усі виклики тихі). План: Kenney Audio CC0 + Freesound → `assets/audio/{sfx,music,voice/uk}`; голос UA — записати 40 фраз (GDD §11).
- Асети сторонні (Kenney, Quaternius CC0) — підключити, коли sandbox працює; кожен у `CREDITS.md`.
- Ріст героя (стадії 2–3) — моделі стадій, «пух!» уже є (`pop_grow`).
- Екран батьків: тижнева статистика, профілі дітей, мова — поки лише сесія/покупка.
- Локалізація: усі рядки поки в коді українською; винести в CSV → Translation.
- Draw calls: **перезаміряно 18.09.2026 — 242** на рівні 1 (було 324–384 напередодні, ~560 у старих оцінках).
  Мета GDD ≤ 150 і далі не досягнута, але розрив утричі менший, ніж вважалося.
  План: [docs/optimisation/2026-09-07](optimisation/2026-09-07-render-budget.md).
- GDD §10 «запуск без меню для young» — зараз меню для всіх (рішення Nick); зафіксувати в GDD v1.2 після плейтесту.

## Борг 3D-прототипу (v0.2.0) — винести в Jira після плейтесту

- Подія «міст падає» (hop, mid+) — у `data/events.json` позначена `_todo`, не реалізована.
- Колоди на річці статичні; за GDD мають пливти (герой їде на колоді).
- Стрибки: 3 доріжки замість 5–9 як у Crossy Road — розширити після рішення щодо камери (GDD §16.7).
- Аудіо/голос: файлів нема, всі виклики безпечні (тиша). Потрібні `assets/audio/...`.
- Погладити героя можна лише на станції (тап поза дверима); екран героїв — вертикальний зріз.
- Сезонний шар і погода (`seasons.json`) — не підключено, небо день→вечір працює.
- Ріст героя (3 стадії) — є `pop_grow()`, немає логіки стадій і даних стадій 2–3.
- `young` з двома механіками (Біг + Хвиля) — перевірити плейтестом; fallback: лише Лужок.

## Стан на 17.09.2026 — що блокує демо

**Головна й єдина справжня перепона: моделі.** Порахунок за фактом (декор + перешкоди,
перешкода бере модель через поле `prop` або за власною назвою):

| світ | рівні | готово | бракує |
|---|---|---|---|
| Лужок | 1–4 | **38 з 38** | — |
| Місто | 12–14 | 17 з 31 | 14 |
| Ліс | 5–8 | 16 з 27 | 11 |
| Пляж | 9–11 | 6 з 21 | 15 |
| Хмаринки | 15–17 | 1 з 17 | 16 |

Різних видів без моделі — **49**. Найдешевший шлях: **Ліс і Місто, 23 види** закривають
сім рівнів. Список із роллю кожного (роль диктує форму: `jump` низький, `duck` високий із
просвітом) — [tasks/props.md](tasks/props.md), розділ «ВИПРАВЛЕННЯ ЛІЧБИ».

Проводки не треба: перешкода шукає модель за власною назвою. Досить покласти `.glb` у
`assets/props/` і додати рядок у `data/props.json`.

### Рішення, ухвалені 17.09.2026

- **Пляж ділиться НАДВОЄ:** перша частина — біг по березі (пісок, пальми, парасолі, замки,
  крабики), далі герой кличе друга-дельфіна, той падає у воду — і друга частина ПІД ВОДОЮ.
  Для коду це більше за моделі: потрібен перехід між станами ВСЕРЕДИНІ рівня (механізму
  немає) плюс визначення «камера під водою» (теж немає). Підводний набір моделей ще не
  складено.
- **Хмаринки — канал прибрано** разом із дерев'яними поручнями й цегляною вежею.

### Зроблено 18.09.2026

- **Миготіння берега — закрито.** Мигтить не форма, а КОНТРАСТ: смуга берега вужча за піксель,
  і важить лише різниця кольорів. `src/run3d/bank.gdshader` веде колір берега в колір трави з
  відстанню — 14 585 → 9 409 пікселів, що стрибають зелене↔коричневе. Z-fighting'у немає:
  проба на паузі показує, що на стоячій картинці змінюються тільки анімовані кролики й піна.
- **Чанки рівнів видно в редакторі.** Локальні координати (`LevelLayout.z_offset_m`), прев'ю
  справжніх моделей замість невидимих маркерів, дорога-орієнтир. Гру це не зачепило: 0 різних
  пікселів на кадрі 400. Знімок розкладки без редактора — `src/debug/chunk_shot.tscn`.
- **Конвеєр моделей готовий.** `prop_bake.py` (ретопологія із запіканням: 30 918 гр. → 1 998
  без втрати вигляду), `prop_budget_search.py` (сам шукає безпечний бюджет), `prop_render.py`.
  Стеля — `tests/test_prop_budget.gd`.
- **Декор у кадрі 247 638 → 123 969 трикутника.** Винуватцем був паркан (78 копій × 2 200), а
  не будинки. Проба тепер пише `decor_cost` — розкладку по шарах.

### Відкрите

- **Моделі — і далі єдина справжня перепона.** 49 видів без моделі (таблиця вище). Конвеєр
  готовий, проводки не треба: `.glb` у `assets/props/` + рядок у `data/props.json`.
- **Пляж надвоє** (берег → дельфін → під водою): потрібен перехід між станами ВСЕРЕДИНІ рівня
  і визначення «камера під водою». Обох механізмів немає — це найбільша відсутня фіча.
- **Аудіо: файлів нема жодного.** ~40 назв sfx/voice уже кличуться з коду й тихо падають у
  порожнечу. Для гри для малят це помітніше за будь-яку графіку.
- **Система розбиття предметів** (ящик розлітається на друзки) — просили давно, не зроблено.
- **Хмари — пласкі коробки** (`Mats.box`).
- **Нічні рівні 8 і 17** не оглянуті як слід: раніше заважало меню поверх кадру (полагоджено
  17.09.2026, огляд тепер дешевий).
- **Бібліотека чанків**, кроки 2–4: опис чанка, збирач рівня зі списку, прогін інваріантів на
  зібраних рівнях — `tasks/level-chunk-library.md`.
- **Гілка зветься `feat/add_voxel_elements`**, хоча воксель прибирають, а не додають.
- **Дрібне:** плоскі `levels/level_XX.tscn` застаріли (правити треба теки);
  `assets/props/3d/bush_flower_1/2` — 47 МБ, які гра не вантажить (у грі це спрайт).
